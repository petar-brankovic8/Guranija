using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DodgabelsSpawner : MonoBehaviour
{
    /*public Dodgable dodgablePrefab;
    public float trajectoryVariance = 15f;
    public float spawnRate = 2.0f;
    public float spawnDistance = 15;
    public int spawnAmount = 1;
    void Start()
    {
        InvokeRepeating(nameof(SpawnDodgable), this.spawnRate, this.spawnRate);
    }

    private void SpawnDodgable()
    {
        for (int i = 0; i < this.spawnAmount; i++)
        {
            Vector2 spawnDirection = Random.insideUnitCircle.normalized * this.spawnDistance;
            Vector2 spawnPoint = this.transform.position + spawnDirection;

            float variance = Random.Range(-this.trajectoryVariance, this.trajectoryVariance);
            Dodgable dodgable = Instantiate(this.dodgablePrefab, spawnPoint)
        }
    }*/
}
